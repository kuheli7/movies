services:
  - type: web
    name: movie-catalog-system
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_TYPE
        value: postgresql
    healthCheckPath: /health

databases:
  - name: movies-db
    databaseName: movies
    user: movie_user
    plan: free
