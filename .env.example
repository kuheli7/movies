# Local Development (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=movies_db

# Production (PostgreSQL) - Render will provide this automatically
DATABASE_URL=postgres://username:password@hostname:port/database
DB_TYPE=postgresql

# Server Configuration
PORT=3000
NODE_ENV=production
